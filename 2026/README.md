# 2026

Active year project.

## Run locally

```bash
composer install
php -S localhost:8000 -t .
```

If the project has frontend assets:

```bash
npm ci
npm run build
```

## Notes

- Keep changes scoped to this folder unless a shared fix is required.
- Use `AGENTS.md` at the repository root for global repo guidance.
